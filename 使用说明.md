# 学习测试控制台使用说明

## 1. 项目定位

本项目是一个面向软件工程测试的课程流程自动化示例，默认启动本地模拟课程平台，使用 Python 和 Selenium 验证以下流程：

```text
前端填写测试配置
        ↓
Python 接收配置
        ↓
浏览器登录本地模拟平台
        ↓
读取课程和章节
        ↓
执行播放与完成动作
        ↓
写入 progress.json
        ↓
前端实时展示状态和日志
```

当前版本默认只允许访问 `127.0.0.1`、`localhost` 和 `::1`。如果有专用授权、测试账号和测试窗口，也可以显式切换到官方测试模式；该模式只允许 HTTPS 的 `chaoxing.com` 子域名，必须打开可见浏览器并由人工完成验证码和登录。

## 2. 环境要求

- Windows、macOS 或 Linux；
- Python 3.8 或更高版本；
- Chrome、Edge 或 Firefox；
- pip；
- 如果使用浏览器流程，需要允许 Selenium Manager 准备浏览器驱动，或者提前把对应驱动放入 `PATH`。
- 交付给其他人时，也可以使用 Docker Desktop 和 Docker Compose v2，镜像会自带 Python、Chromium、ChromiumDriver 和中文字体。

## 3. 安装项目

在 PowerShell 中进入项目目录：

```powershell
cd F:\CODEX\学习通
```

建议创建虚拟环境：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

如果 PowerShell 不允许执行虚拟环境脚本，可以直接使用当前 Python，或者在得到管理员许可后调整执行策略。不要为了解决这个问题关闭系统安全功能。

安装依赖：

```powershell
python -m pip install -r requirements.txt
```

### Docker 一键运行

如果不想在每台电脑上单独安装 Python、Selenium 和浏览器，可以直接使用项目内的 Docker 封装。Docker 默认启动本地模拟平台和无头 Chromium，不访问外部平台：

```powershell
Copy-Item .env.docker.example .env
docker compose up --build -d
```

然后打开：

```text
http://127.0.0.1:8787
```

查看状态和日志：

```powershell
docker compose ps
docker compose logs -f workbench
```

验证容器内的本地接口：

```powershell
docker compose run --rm --no-deps workbench python main.py self-test
```

容器会把运行结果挂载到项目的 `runtime/` 目录，包含日志、异常截图、`progress.json` 和 `playback_report.json`。关闭容器时只执行 `docker compose down` 即可保留这些数据。

Docker 镜像默认使用 Chromium 和 `HEADLESS=true`。官方授权测试需要人工完成验证码和登录，容器默认没有可见桌面，因此建议使用宿主机的 Edge/Chrome 可见模式；不要把生产账号、Cookie 或密码写进 Dockerfile，也不要提交 `.env`。

使用 Docker 时，不需要再执行下面的 Python 虚拟环境和 `pip` 安装步骤；本地 Python 方式才继续使用下面的 `.env.example`。

复制配置模板：

```powershell
Copy-Item .env.example .env
```

## 4. 配置 `.env`

用文本编辑器打开项目根目录的 `.env`：

```powershell
notepad .env
```

本地模拟平台可以使用下面的测试配置：

```env
CHAOXUN_USERNAME=test_user
CHAOXUN_PASSWORD=test_password
CHAOXUN_URL=http://127.0.0.1:8765
TEST_TARGET=local
ALLOW_OFFICIAL_TEST=false
OFFICIAL_URL=https://v8.chaoxing.com
MOCK_SERVER=true
BROWSER=chrome
HEADLESS=false
BROWSER_TIMEOUT=30
LESSON_SECONDS=1.0
PLAYBACK_MINUTES=10.0
OFFICIAL_PLAYBACK_SCOPE=latest_unfinished
OFFICIAL_MAX_LESSONS=1
OFFICIAL_TASK_POINTS=2
OFFICIAL_PLAYER_WAIT_SECONDS=5
OFFICIAL_PLAYBACK_RATE=2
COURSE_IDS=
LOG_LEVEL=INFO
LOG_FILE=logs/chaoxun.log
SCREENSHOT_DIR=screenshots
PROGRESS_FILE=progress.json
REPORT_FILE=playback_report.json
# 可选：留空时使用页面语义探测，不把页面结构写进 Python 代码
OFFICIAL_USERNAME_SELECTOR=
OFFICIAL_PASSWORD_SELECTOR=
OFFICIAL_LOGGED_IN_SELECTOR=
OFFICIAL_COURSE_SELECTOR=
OFFICIAL_LESSON_SELECTOR=
OFFICIAL_PLAY_SELECTOR=
MOCK_HOST=127.0.0.1
MOCK_PORT=8765
```

重要配置说明：

| 配置项 | 说明 |
| --- | --- |
| `CHAOXUN_USERNAME` | 测试用户名。默认是 `test_user`。 |
| `CHAOXUN_PASSWORD` | 测试密码。默认是 `test_password`。不要提交到 Git。 |
| `CHAOXUN_URL` | 当前测试目标。本地模式必须是回环地址；官方模式必须是 HTTPS 的 `chaoxing.com` 子域名。 |
| `TEST_TARGET` | `local` 使用本地模拟平台；`official` 使用官方授权测试适配器。 |
| `ALLOW_OFFICIAL_TEST` | 官方模式显式开关，默认 `false`。 |
| `OFFICIAL_URL` | 官方模式默认登录入口，默认 `https://v8.chaoxing.com`。 |
| `MOCK_SERVER` | `true` 时自动启动本地模拟课程平台。 |
| `BROWSER` | `chrome`、`edge` 或 `firefox`。 |
| `HEADLESS` | `true` 表示无头运行，`false` 表示显示浏览器窗口。 |
| `BROWSER_TIMEOUT` | 页面元素等待和加载超时时间，单位为秒。 |
| `LESSON_SECONDS` | 每节本地测试章节的等待时长，单位为秒。 |
| `PLAYBACK_MINUTES` | 官方模式每个章节的播放观测时长，单位为分钟，范围 0.1–60 分钟。 |
| `OFFICIAL_PLAYBACK_SCOPE` | 官方模式播放范围，默认只取最新的未完成章节；也可设为 `all_unfinished`。 |
| `OFFICIAL_MAX_LESSONS` | 单次官方测试最多观测的章节数，范围 1–20，默认 1。 |
| `OFFICIAL_TASK_POINTS` | 学习通目录剩余任务点数（`.orangeNew`），默认 2；0 表示任意未完成。 |
| `OFFICIAL_PLAYER_WAIT_SECONDS` | 切换章节后等待嵌套播放器加载，默认 5 秒。 |
| `OFFICIAL_PLAYBACK_RATE` | 官方播放倍速，默认 2，最高 2。 |
| `COURSE_IDS` | 课程 ID，多个 ID 用英文逗号分隔，例如 `1,2`；留空表示全部课程。 |
| `REPORT_FILE` | 官方播放结果 JSON 路径。 |
| `OFFICIAL_USERNAME_SELECTOR` | 官方登录用户名选择器，可留空使用输入类型和属性探测。 |
| `OFFICIAL_PASSWORD_SELECTOR` | 官方登录密码选择器，可留空使用密码输入框探测。 |
| `OFFICIAL_LOGGED_IN_SELECTOR` | 可选的登录完成标志选择器。 |
| `OFFICIAL_COURSE_SELECTOR` | 可选的课程卡片/链接选择器。 |
| `OFFICIAL_LESSON_SELECTOR` | 可选的章节卡片/链接选择器。 |
| `OFFICIAL_PLAY_SELECTOR` | 可选的播放控件选择器。 |
| `LOG_FILE` | 日志文件路径。 |
| `SCREENSHOT_DIR` | 浏览器异常截图目录。 |
| `PROGRESS_FILE` | 学习进度 JSON 文件路径。 |

如果前端密码框留空，Python 会使用 `.env` 中的 `CHAOXUN_PASSWORD`。如果在前端临时输入密码，前端提交后会清空密码框，不会写入浏览器 `localStorage`。

## 5. 第一次运行前的自检

先运行不需要浏览器的接口自检：

```powershell
python main.py self-test
```

自检包括：

1. 外部目标地址是否被拒绝；
2. 本地健康接口是否正常；
3. 测试账号能否登录；
4. 是否能读取课程列表；
5. 未开始播放时是否禁止完成章节；
6. 播放后是否能在最短时长后完成章节；
7. 连续失败登录是否触发 `429 Too Many Requests`。

看到下面的结果，说明接口层自检通过：

```text
自检通过: 本地目标限制、登录、课程读取、播放前置条件和登录限流
```

也可以运行单元测试：

```powershell
python -m unittest discover -s tests -v
```

## 6. 使用 Web 前端

### 6.1 启动控制台

```powershell
python main.py ui
```

默认会启动两个本地服务：

```text
课程模拟平台：http://127.0.0.1:8765
Web 控制台：http://127.0.0.1:8787
```

打开浏览器访问：

```text
http://127.0.0.1:8787
```

如果 8787 端口已被占用，可以换一个控制台端口：

```powershell
python main.py ui --port 8788
```

然后访问：

```text
http://127.0.0.1:8788
```

### 6.2 配置页面

页面分成四个区域：

#### 测试配置

填写或确认：

- 测试用户名；
- 测试密码；
- 浏览器类型；
- 每节测试时长（本地为秒，官方为分钟）；
- 是否使用无头模式。

选择测试环境：

- `Local Mock`：目标显示 `LOOPBACK`，课程来自本地模拟平台；
- `Official Test`：只有 `.env` 已开启 `ALLOW_OFFICIAL_TEST=true` 时可选，目标显示 `ALLOWLIST`，浏览器会保持可见。

官方模式下，课程不会在启动前写死在页面里。可以在“官方测试课程筛选”中输入课程 ID 或名称，多个值用英文逗号分隔；留空则由 Python 在人工登录完成后探测候选课程。页面还会显示任务点数、播放器等待、倍速、单次/循环范围和最多章节，对应 ChaoXing_AutoStudy 插件的浮窗设置。

#### 测试课程

页面会列出本地模拟平台的课程：

- `Python基础`，课程 ID 为 `1`，包含 3 个章节；
- `软件工程测试`，课程 ID 为 `2`，包含 2 个章节。

默认全部勾选。取消某门课程后，本次任务只执行剩余勾选项。全部取消时，程序按“留空表示全部课程”的规则执行全部课程。

#### 执行现场

这里会显示：

- 当前任务状态；
- 当前课程和章节；
- 当前章节完成数量；
- 当前课程进度百分比；
- Python 执行器回传的事件。

状态含义：

| 状态 | 含义 |
| --- | --- |
| `待机` | 尚未提交任务。 |
| `运行中` | Python 正在启动浏览器或处理章节。 |
| `已完成` | 所选课程执行成功。 |
| `失败` | 至少有一个课程处理失败，需要查看日志和截图。 |

#### 事件流和测试护栏

事件流显示当前任务的简要日志。测试护栏显示当前版本启用的限制：

- `NO EXTERNAL TARGET`：拒绝外部目标；
- `NO STEALTH`：不隐藏 Selenium 自动化特征；
- `NO CAPTCHA BYPASS`：不绕过验证码。

官方模式额外显示 `MANUAL CAPTCHA` 和 `NO COMPLETION WRITE`，表示验证码由人工完成，测试不会调用完成、打卡或进度写入接口。

### 6.3 启动任务

配置完成后点击：

```text
启动测试任务
```

Python 会在后台线程中执行任务，前端每 800 毫秒查询一次 `/api/status`，因此不需要刷新页面。

任务期间不要关闭运行 `python main.py ui` 的 PowerShell 窗口。要停止控制台，回到该窗口按：

```text
Ctrl+C
```

### 6.4 官方授权测试流程

只在得到平台方明确授权、使用专用测试账号和测试课程时执行。配置示例：

```env
TEST_TARGET=official
ALLOW_OFFICIAL_TEST=true
CHAOXUN_URL=https://v8.chaoxing.com
OFFICIAL_URL=https://v8.chaoxing.com
MOCK_SERVER=false
HEADLESS=false
PLAYBACK_MINUTES=10
OFFICIAL_PLAYBACK_SCOPE=latest_unfinished
OFFICIAL_MAX_LESSONS=1
OFFICIAL_TASK_POINTS=2
OFFICIAL_PLAYER_WAIT_SECONDS=5
OFFICIAL_PLAYBACK_RATE=2
COURSE_IDS=
```

启动 `python main.py ui` 后，在前端选择 `Official Test`，填写测试账号、密码和可选课程筛选，点击“启动测试任务”。实际操作顺序如下：

```text
前端输入测试账号和密码
        ↓
Python 通过可见浏览器填写官方登录页
        ↓
验证码由人工完成，并由人工点击登录
        ↓
Python 读取测试课程，并进入学习通目录页
        ↓
按任务点数匹配未完成章节，点击目录后等待播放器加载
        ↓
在嵌套 iframe 中以二倍速播放，观察 video 状态、进度资源和风控提示
        ↓
生成 playback_report.json，并在“播放结果”卡片中显示
```

这条流程不做以下事情：不识别或绕过验证码、不隐藏 Selenium、不伪造请求、不点击“完成”、不提交作业、不写入学习进度。官方模式的网络观察只记录匹配资源的路径、HTTP 状态和资源类型，去除查询参数，避免把 Cookie 或临时令牌写入报告。

如果课程页面结构变化，可以在 `.env` 中给当前授权测试环境提供 `OFFICIAL_COURSE_SELECTOR`、`OFFICIAL_LESSON_SELECTOR` 或 `OFFICIAL_PLAY_SELECTOR`。不需要把课程名称、账号或密码硬编码到 Python 源码。

官方模式在学习通课程页会读取 `.posCatalog_select` 目录，并用 `.orangeNew` 任务点数筛选未完成章节；切换后等待播放器加载，再对嵌套 `video#video_html5_api` 设置倍速并播放。前端「单次 / 下一节未完成」对应插件单次播放，「循环 / 全部未完成」对应循环播放，但仍受 `OFFICIAL_MAX_LESSONS` 上限约束。已完成或状态未知的章节不会被自动播放。项目不提供无人值守的无限循环；需要持续运行时，应改为只读状态监测和报告。

报告中的常见字段：

- `summary.played`：观察到媒体实际前进的章节数；
- `summary.failed`：没有读到章节、播放控件、video 或播放时间的项目数；
- `results[].progress_observations`：页面加载期间发现的进度/学习相关资源；
- `results[].risk_observations`：匹配到的错误 HTTP 响应；
- `results[].risk_markers`：页面可见的风控或验证提示。

报告是诊断证据，不等同于平台学习记录，也不等同于生产验收。
```

## 7. 不使用前端时的命令行运行

执行一次任务：

```powershell
python main.py run
```

无头运行：

```powershell
$env:HEADLESS='true'
python main.py run
```

指定测试时长：

```powershell
$env:LESSON_SECONDS='0.5'
python main.py run
```

这里的测试时长是本地模拟平台的验证等待时间，不代表真实课程学习时长。

## 8. 定时任务

每天 08:00 执行：

```powershell
python main.py schedule
```

每天 09:00 执行：

```powershell
python main.py schedule 09:00
```

定时器使用 Python 标准库实现。程序启动后会等待到指定时间；关闭 PowerShell 窗口或按 `Ctrl+C` 可以停止。

定时命令只适用于本地模拟回归。官方模式不允许通过 `schedule` 持续自动播放；官方测试必须由人工完成验证码，并且每次只运行有上限的测试会话。

## 9. 查看运行结果

### 日志

日志默认在：

```text
logs/chaoxun.log
```

查看最近 50 行：

```powershell
Get-Content logs\chaoxun.log -Tail 50
```

持续查看：

```powershell
Get-Content logs\chaoxun.log -Tail 50 -Wait
```

只查看错误：

```powershell
Select-String -Path logs\chaoxun.log -Pattern ERROR
```

### 异常截图

浏览器流程异常时，截图会保存到：

```text
screenshots/
```

截图文件名包含时间和课程 ID，例如：

```text
20260913_171150_course_1.png
```

### 进度文件

进度默认保存在：

```text
progress.json
```

示例：

```json
{
  "courses": [
    {
      "id": 1,
      "name": "Python基础",
      "url": "http://127.0.0.1:8765/course/1",
      "progress": 100.0
    }
  ],
  "progress": {
    "Python基础": {
      "completed": 3,
      "total": 3,
      "percentage": 100.0
    }
  }
}
```

程序使用临时文件替换方式写入。如果 JSON 已损坏，程序会报错并保留原文件，不会静默覆盖损坏内容。

## 10. 常见问题

### 10.1 浏览器打不开或 Selenium 报错

检查：

1. Chrome 或 Firefox 是否已经安装；
2. 是否执行了 `python -m pip install -r requirements.txt`；
3. Selenium Manager 是否能访问驱动下载地址；
4. 是否有其他浏览器进程阻塞测试配置；
5. `BROWSER` 是否填写为 `chrome`、`edge` 或 `firefox`。

如果环境不能自动准备驱动，可以手动安装对应版本的 ChromeDriver 或 GeckoDriver，并把驱动目录加入 `PATH`。

### 10.2 前端打不开

先确认是否启动了 Web 控制台：

```powershell
python main.py ui
```

再检查端口：

```powershell
Get-NetTCPConnection -LocalPort 8787 -State Listen
```

确认本机浏览器访问：

```text
http://127.0.0.1:8787
```

`127.0.0.1` 只代表运行 Python 程序的这台机器。在手机、另一台电脑或远程浏览器中访问时，它不会指向这台运行项目的电脑。

### 10.3 页面显示没有课程

检查：

```powershell
Invoke-WebRequest http://127.0.0.1:8787/api/courses -UseBasicParsing
Invoke-WebRequest http://127.0.0.1:8765/health -UseBasicParsing
```

如果模拟平台没有启动，确认 `.env` 中有：

```env
MOCK_SERVER=true
```

### 10.4 任务失败但浏览器已经启动

依次查看：

1. 前端事件流；
2. `logs/chaoxun.log`；
3. `screenshots/` 下最新截图；
4. 当前 `.env` 中的测试用户名和密码；
5. `BROWSER_TIMEOUT` 是否太小。

程序不会通过隐藏错误来报告成功；任意课程失败都会让任务进入失败状态并返回非零退出码。

### 10.5 官方模式为什么仍需要显式开关

官方模式涉及外部站点，因此 `config.py` 和 Web API 都要求 `ALLOW_OFFICIAL_TEST=true`、可见浏览器和 HTTPS `chaoxing.com` 域名。默认仍是本地模拟模式，避免把测试账号、Cookie 或课程记录误发到生产站点。

如果要接入官方授权测试环境，应先明确：

- 专用测试域名或测试入口；
- 测试租户、测试账号和课程范围；
- 允许的操作和测试时间窗口；
- 页面结构或官方测试 API 契约。

本版本已经提供独立的官方播放诊断适配器，但不会把生产账号、课程或页面数据写死在代码中；页面结构差异通过 `.env` 选择器配置或后续适配处理。

## 11. 数据和安全注意事项

- `.env` 不要提交到 Git；
- 不要把密码、Cookie、Token 粘贴到问题反馈或日志中；
- 不要把 `progress.json`、异常截图或日志上传到公共仓库；
- 不要在前端 `localStorage` 中保存密码；
- 不要添加隐藏浏览器自动化特征的代码；
- 不要绕过验证码、风控、登录保护或访问控制；
- 测试只使用专用测试账号和测试数据。

## 12. 推荐验收顺序

```powershell
# 1. 依赖和接口
python main.py self-test

# 2. 单元测试
python -m unittest discover -s tests -v

# 3. 启动前端
python main.py ui

# 4. 浏览器打开
Start-Process http://127.0.0.1:8787

# 5. 在页面中选择课程并启动测试

# 6. 查看日志、截图和进度
Get-Content logs\chaoxun.log -Tail 50
Get-Content progress.json
```
